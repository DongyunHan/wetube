extends layouts/main

block content
    .form-container    
        form(action=routes.editProfile, method="post")
            input(type="file", id="videoFile", name="videoFile")
            input(type="text", name="videoTitle", placeholder="Title")
            input(tpye="email", name="videoDescription", placeholder="Description")
            input(type="submit", value="Edit Profile")

        a.form-container__link(style="color: #f0ad4e",href=routes.deleteVideo) Delete Video